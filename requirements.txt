# Data Generator
faker==28.4.1
psycopg2-binary==2.9.9
PyYAML==6.0.1

# Kafka Consumer (Phase 2)
confluent-kafka==2.3.0
boto3==1.34.0          # MinIO/S3

# Airflow (Phase 3 — install separately or use Docker image)
# apache-airflow==2.8.0
# apache-airflow-providers-snowflake==5.3.0
# apache-airflow-providers-amazon==8.13.0

# dbt (Phase 4 — install in venv)
# dbt-snowflake==1.7.0
